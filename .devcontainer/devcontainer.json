{
    "updateContentCommand": "npm install",
    "postStartCommand": "npx --yes pm2 start server/index.js",
    "remoteEnv": {
        "PORT": "${PORT}",
        "MONGO_URI": "${MONGO_URI}",
        "NEXT_PUBLIC_ONCHAINKIT_API_KEY": "${NEXT_PUBLIC_ONCHAINKIT_API_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
    }
}